{
	"name": "board1",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "apps/board1/src",
	"projectType": "application",
	"targets": {
		"build": {
			"executor": "@nx/vite:build",
			"outputs": ["{options.outputPath}"],
			"defaultConfiguration": "production",
			"options": {
				"outputPath": "apps/board1/dist"
			},
			"configurations": {
				"development": {
					"mode": "development"
				},
				"production": {
					"mode": "production"
				}
			}
		},
		"dev": {
			"executor": "@nx/vite:dev-server",
			"defaultConfiguration": "development",
			"options": {
				"buildTarget": "board1:build"
			},
			"configurations": {
				"development": {
					"buildTarget": "board1:build:development",
					"hmr": true
				},
				"production": {
					"buildTarget": "board1:build:production",
					"hmr": false
				}
			}
		}
	},
	"tags": []
}
